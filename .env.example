# Neon Postgres — use the pooled (pgbouncer) connection string
# Format: postgres://USER:PASS@HOST-pooler.REGION.aws.neon.tech/DB?sslmode=require
DATABASE_URL=

# OpenAI API key — requires access to gpt-4o-mini
OPENAI_API_KEY=
